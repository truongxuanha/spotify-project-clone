import classNames from 'classnames/bind';
import BoxcardMusic from '~/components/BoxcardMusic';
import styles from './Search.module.scss';

const cx = classNames.bind(styles);

function Search() {
    return <BoxcardMusic />;
}

export default Search;
